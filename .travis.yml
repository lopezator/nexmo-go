language: go
go_import_path: github.com/lopezator/nexmo-go

os: linux
dist: xenial

before_script:
  - go get -u -t ./...

script:
  - make prepare
  - make sanity-check
  - make test

cache:
  directories:
    - $GOPATH/pkg
